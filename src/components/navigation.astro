---
const { currentPage = "/" } = Astro.props;
const pages = [
  { path: "/", label: "Home" },
  { path: "/about", label: "About" },
  { path: "/blog", label: "Blog" },
];
---

<nav class="main-nav">
  <div class="container">
    <a href="/" class="logo">My Astro Site</a>

    <ul>
      {
        pages.map((page) => (
          <li>
            <a href={page.path}>{page.label}</a>
          </li>
        ))
      }
    </ul>
  </div>
</nav>

<style>
  .main-nav {
    background: #0f172a;
    color: white;
    padding: 1rem 0;
    margin-bottom: 2rem;
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 1.5rem;
  }

  .logo {
    font-size: 1.5rem;
    font-weight: bold;
    color: #e2e8f0;
    text-decoration: none;
  }

  ul {
    display: flex;
    gap: 2rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  a {
    color: #cbd5e1;
    text-decoration: none;
    font-weight: 500;
    transition: color 0.2s;
  }

  a:hover {
    color: white;
  }

  .active a {
    color: #60a5fa;
    border-bottom: 2px solid #60a5fa;
    padding-bottom: 4px;
  }

  @media (max-width: 640px) {
    .container {
      flex-direction: column;
      gap: 1rem;
    }
  }
</style>
